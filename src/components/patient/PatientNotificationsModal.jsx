import HospitalNotificationsModal from '../common/HospitalNotificationsModal';

export default function PatientNotificationsModal({ isOpen, onClose }) {
  return <HospitalNotificationsModal isOpen={isOpen} onClose={onClose} />;
}
